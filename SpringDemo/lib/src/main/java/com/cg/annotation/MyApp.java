package com.cg.annotation;

import org.springframework.context.support.ClassPathXmlApplicationContext;

public class MyApp {

	public static void main(String[] args) {
		
		
		
		
		
		
		
		
		/*
		// Annotation example:
		// Load the configuration into the SpringContainer/ApplicationContext
	    ClassPathXmlApplicationContext context = 
					    new ClassPathXmlApplicationContext("applicationContext.xml");
		Employee emp = context.getBean("employeeComponent", Employee.class);
		Employee emp1 = context.getBean("employeeComponent", Employee.class);
		System.out.println(emp==emp1);//true
		
		Address addr = context.getBean("address", Address.class);
		addr.setId(2001);
		addr.setCity("Bangalore");
		
		emp.setEmpId(1001);
		emp.setEmpName("Ram");
		
		System.out.println(emp.getEmpId());
		System.out.println(emp.getEmpName());
		System.out.println(emp.getAddress());
		context.close();
		
		*/

	}
}